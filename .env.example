# 复制本文件为 .env 并填写真实信息（.env 已被 .gitignore 排除，不会提交到 Git）
# 切勿把真实 JWT 写进代码或提交到公开仓库（JWT 内含你的学号、姓名）

# ===== 1. 抓包得到的完整 JWT（从学校 APP 缴费请求头 Cookie 中复制） =====
XJTU_CEMS_JWT=eyJhbGciOiJSUzUxMiJ9.eyJxxx.yyy你的完整JWT

# ===== 2. 宿舍房间号（创新港 roomId） =====
XJTU_ROOM_ID=<你的roomId>

# ===== 3. 收件邮箱（接收所有通知/报告的邮箱） =====
#   填你实际使用的邮箱，例如 123456@qq.com；也可以填下面的发件人自身（自收发）。
XJTU_RECIPIENT=you@example.com

# ===== 4. 网页报告公网链接（可选，用于二维码） =====
#   把 deploy/ 部署到 CloudStudio / GitHub Pages / Vercel 后，把链接填这里；
#   留空则二维码为空白（可后续补填后重跑生成）。
XJTU_SHARE_URL=

# ===== 5. agently-cli 可执行文件完整路径（各机器不同） =====
#   先 `npm i -g agently-cli`，再填它的绝对路径（Windows 通常为 *.cmd）。
#   找不到命令时必填；常见位置：
#     Windows: C:\Users\你的用户名\AppData\Roaming\npm\agently-cli.cmd
#     macOS/Linux: $(npm root -g)/.bin/agently-cli  或  $(which agently-cli)
AGENTLY_BIN=

# ============================================================
# 关于「发件邮箱」：本项目通过 Agent Mail CLI（agently-cli）发信，
# 发件人 = 你执行 `agently-cli auth login` 时 OAuth 授权的「智能体邮箱」
#   （形如 xxx@agent.qq.com，是你自己的，不是本仓库作者的 smart5@agent.qq.com）。
# 因此只需在克隆后自行 `agently-cli auth login` 授权，无需在 .env 里配置发件人。
# 收件人则务必在上方 XJTU_RECIPIENT 改成你自己的邮箱。
# ============================================================
