# 运行 dorm_elec_auto.py 所需的第三方依赖（与脚本顶部 PEP 723 声明一致）。
# 适用于未使用 uv / PEP 723 运行器的环境：
#   pip install -r requirements.txt
requests
matplotlib
numpy

# 打包成 exe 所需（仅构建期需要，运行不需要）：
#   pip install pyinstaller
pyinstaller

